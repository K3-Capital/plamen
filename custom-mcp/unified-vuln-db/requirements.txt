# Core
mcp>=1.0.0
chromadb>=0.4.0
pydantic>=2.0.0

# Embeddings - Code Aware (priority order)
sentence-transformers>=2.2.0  # For Nomic/CodeBERT/MiniLM
einops>=0.7.0                 # Required by nomic-embed-text-v1.5 (trust_remote_code)
# voyageai>=0.2.0  # Optional: uncomment if you have VOYAGE_API_KEY

# Data fetching
httpx>=0.24.0
aiofiles>=23.0.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
datasets>=2.14.0  # For HuggingFace

# Rate limiting & retry
tenacity>=8.2.0
ratelimit>=2.2.0

# Utils
anyio>=4.0.0
click>=8.1.0
rich>=13.0.0
python-dotenv>=1.0.0

# Note: torch and transformers are installed automatically by sentence-transformers
# To use Voyage Code embeddings (API-based, best quality), uncomment:
# voyageai>=0.2.0  # Requires VOYAGE_API_KEY env var
